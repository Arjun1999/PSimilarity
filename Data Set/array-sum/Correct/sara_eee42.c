#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{ int n,t,a;
 scanf("%d",&n);
 while(n--)
   {
     scanf("%d",&a);
     t=t+a;
 }
printf("%d",t);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
