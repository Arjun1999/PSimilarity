#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    scanf("%d", &N);
    int A[N];
    int i, sum=0;
    for(i=0; i<N; i++){
    scanf("%d", &A[N]);
        sum=sum+A[N];
    }
    printf("%d", sum);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
