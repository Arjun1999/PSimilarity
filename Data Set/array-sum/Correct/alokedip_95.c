#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int i,n,a[1000],p=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        {
scanf("%d",&a[i]);
    p+=a[i];}
printf("%d",p);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
