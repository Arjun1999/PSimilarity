#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,a,b=0;
    scanf("%d",&n);
    while(n--)
        {
        scanf("%d",&b);
        a = a + b;
    }
    printf("%d",a);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
