#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int i,k=0;
    int temp,sum=0;
    for(scanf("%d",&i);k<i;k++)
        {
        scanf("%d",&temp);
        sum +=temp;
        
    }
    printf("%d",sum);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
