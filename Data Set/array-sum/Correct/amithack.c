#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    
    int n,a[n],sum=0,i;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&a[n]);
        sum=sum+a[n];
        }
    printf("%d",sum);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
