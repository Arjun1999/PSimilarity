#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
   unsigned long long int n,a[1000],i,sum=0;
    scanf("%lld\n",&n);
     if(0<a[i]<1000){
    for(i=0;i<n;i++)
        {
        scanf("%lld\t",&a[i]);
        
    }
    for(i=0;i<n;i++)
        {
        sum=sum+a[i];
        
    }
   printf("%lld",sum);    
     }
      

        
    return 0;
}
