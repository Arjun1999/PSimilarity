#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {long long int i,j,n,sum=0; 
scanf("%lld",&n);for(i=0;i<n;i++){scanf("%lld",&j);sum=sum+j;}printf("%lld",sum);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
