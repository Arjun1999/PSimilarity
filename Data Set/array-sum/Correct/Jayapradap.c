#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
    int a=0,b,t,i,c;
       
    scanf("%d",&t);
    for(i=1;i<=t;i++)
        {
        scanf("%d",&b);
       // a=b+a;
        a=a+b;
        
    }
printf("%d",a);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
