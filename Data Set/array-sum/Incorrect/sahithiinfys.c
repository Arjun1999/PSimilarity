#include<stdio.h>
int main ()
{
    int size , array;
    scanf("%d",size);
    for ( i = 0 ; i <= size ; i++)
    {
        scanf("%d",array[i]);
    }
    sum = 0;
    for ( i= 0 ; i <= size ; i++)
    {
        sum = sum + a[i];
    }
    printf("%d",sum);
}
