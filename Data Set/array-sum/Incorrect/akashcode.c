#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int a[n],i,n,sum=0;
    printf("enter the size of array");
    scanf("%d",&n);
    printf("enter the elemenrs of array");
    for(i=o;i<=n;i++)
        {scanf("%d"a[i]);
         sum=sum+a[i];
        }
    printf("%d"&sum);
    
    return 0;
}
