#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main(){
    int n,c,arr[i]; 
    scanf("%d",&n);
    int arr[n];
    for(int arr_i = 0; arr_i < n; arr_i++){
       scanf("%d",&arr[arr_i]);
        printf("%d",n);
    printf("%d",arr[i]);
            c=i[0]+i[1]+i[2]+i[3]+i[4]+i[5];
      printf("%d",c);      
    }
    return 0;
}
