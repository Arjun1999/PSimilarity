#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    int arr[5],i,sum=0;
    printf("enter elements of array");
    for(i=0;i<5;i++)
         scanf("%d",&arr[i]);
    for(i=0;i<5;i++)
         sum=sum+a[i];
    printf("sum=%d",sum);
    return 0;
}
