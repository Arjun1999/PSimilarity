#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>
#include<stdio.h>

int main();
{
 int sum=0;   
    int i;
    printf("Enter sum of elements");
    scanf("%d",&n);
    int arr[n];
    for(int arr_i = 0; arr_i < n; arr_i++){
        sum+=arr[i];
        printf("sum of elements is %d",sum(arr,n));
       scanf("%d",&arr[arr_i]);
    }
    return 0;
}
