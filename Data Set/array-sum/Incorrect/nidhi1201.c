#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
 
    int n,arr[n];
    int i,sum;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        {
        sum+=arr[i];
        printf("%d",sum);
        
    }
      
    return 0;
}
