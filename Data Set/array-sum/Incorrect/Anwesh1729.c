#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {int a[50],n,i,sum=0;
            scanf("%d",&n);
            for(i=0;i<=50;i++)
                printf("%d",&n);
            while(i<=50)
                {
              sum=sum+i;
                i++;
            }
            printf("sum=%d",sum);
            
    

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
