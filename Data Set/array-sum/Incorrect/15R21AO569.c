#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main() 
{
    int n,sum=0,i,a;
    scanf("%d",&n);
    for(i=1;i<n;i++)
        {
        scanf("%d",&a);
   sum=sum+x;
        }
        printf("%d",sum);    
    return 0;
}
