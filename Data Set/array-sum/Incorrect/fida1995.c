#include <stdio.h>
#include <math.h>
int main() {
    int i;
    int N;
    int sum;
    int a;
    scanf("%d",N);
for(i=0,i<N,i++)
    {
    scanf("%d"&a);
    sum=sum+a;
}
    return sum;
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
