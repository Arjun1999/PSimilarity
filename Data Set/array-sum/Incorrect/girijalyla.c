#include <assert.h>
#include <limits.h>
#include <math.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){
    int a[i],n,sum=0;
    printf("enter the value of n");
    scanf("%d",&n);
    for(i=0;i<n;i++){
        a[i];
        printf("enter the value for i=0 ",%d);
        sum=sum+a[i];

    }
    printf("the value of sum is %d",%d);
}
