#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
int a[56][56],sum=0,n,i,j;
    scanf("%d,&n");
    for(i=0,i>n,i++)
        {
        for(j=0,j>n,j++)
            {
            sum=sum+a[i][j];
        }
    }
    printf("%d,sum");
        }
    getch();
}

