#include<stdio.h>
int main()
{
    int a[i],n=1000,sum=0;
    printf("enter the array size");
    scanf("%d",n);
    printf("enter the array elements");
    for(i=0;i<n;i++)
    {
      printf("the elements is %d",a[i]);
    }
    sum=sum+a[i];
    return 0;
    
}