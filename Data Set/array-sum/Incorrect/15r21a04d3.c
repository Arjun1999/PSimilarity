#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
   int array[6],i,sum=0,*ptr;
    printf("enter 6 numbers");
    scanf("%d",&array[i]);
    for(i=0;i<=6;i++)
        ptr=array;
    for(i=0;i<=6;i++);
        {
        sum=sum+*ptr;
        ptr++;
    }
    printf("%d",sum);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
