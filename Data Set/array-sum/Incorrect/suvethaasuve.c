#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int q[6],i,sum=0;
    for(i=0;i<6;i++)
        {
        scanf("%d",&q[i])
        }
    for(i=0;i<6;i++)
        {
        sum=sum+q[i];
       
    }
    printf("%d",sum);

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}
