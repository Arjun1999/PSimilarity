#include<stdio.h>
int main()
{
        int i;
        int max,min,a[5],sum=a[0]+sum;
        scanf("%11d",&a[0]);
        sum=maxa[0],sum=mina[0],sum=a[0]+sum;
        for(i=1;i<5;i++)
        {
                if(max<a[i])
                max=a[i];
                if(min>a[i])
                min=a[i];
                sum=sum+a[i];
        }
        printf("%11d %11d",sum-max,sum-min);
        return 0;
}