#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>
int main(){
    long int a; 
    long int b;
    int min;
    int max;
    long int c; 
    long int d;
    long int e;
    scanf("%lld %lld %lld %lld %lld",&a,&b,&c,&d,&e);
    min=a + b + c+ d+ e;
    max=b + c + d + e + f;
    printf(min,max);
    return 0;
}
