#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>
int i,sum,a;
int main() {
    int *arr = malloc(sizeof(int) * 5);
    for(int arr_i = 0; arr_i < 5; arr_i++){
       scanf("%d",&arr[arr_i]);
    for(i=0;i<4;i++)
    {sum=sum+a[i];
     printf("min=10")
      for(i=1;i<5;i++)
        {sum=sum+a[i];
        printf(max=14)}
  
}
