#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

void miniMaxSum(int arr_size, int* arr) {
    using namespace std;

int main() 
{
int val,i;
long long int sum=0;

int a[i];

for(i=0; i<5; i++)
{
        scanf("%d",&val);
        sum+=val;
        printf("%d",sum);
        array.push_back(val);
}
printf("%d",endl);

        sort(array.begin(), array.end());   
        
for(i=0; i<5; i++)
{
  printf("%d,a[i]");
}
        printf("%d,%d",sum-array[4],sum-array[0];  
        return 0;
}


int main() {
    int *arr = malloc(sizeof(int) * 5);
    for (int arr_i = 0; arr_i < 5; arr_i++) {
       scanf("%i",&arr[arr_i]);
    }
    miniMaxSum(5, arr);
    return 0;
}
