#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    int m,i,k,j,temp;
   
    int *arr = malloc(sizeof(int) * 5);
    for(int arr_i = 0; arr_i < 5; arr_i++){
       scanf("%d",&arr[arr_i]);
    }
    while(i<5)
    {
        printf("f");
        if(i!=k)
        {
            printf("p");
            
            a[m]=a[m]+arr[i];
            
            
        }
        i++;
        k++;
        m++;
        a[m]=0;
        
    }
    for(i=0;i<5;i++)
    {
        printf("%d",a[i]);
    }
}
  
   