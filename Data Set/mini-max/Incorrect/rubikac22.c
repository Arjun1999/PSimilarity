#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int {
    int i,a[5],sum[5];
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i!=0;i<n;i++)
    {
       sum[0]=sum[0]+a[i];
    }
    for(i=0;i!=1;i<n;i++)
    {
       sum[0]=sum[0]+a[i];
    }
    for(i=0;i!=2;i<n;i++)
        {
         sum[0]=sum[0]+a[i];
    }
    for(i=0;i!=3;i<n;i++)
    {
        sum[0]=sum[0]+a[i];
    }
    for(i=0;i!=4;i<n;i++)
        {
     sum[0]=sum[0]+a[i];
    }
    for
    return 0;
}
